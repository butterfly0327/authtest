<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.backend.mapper.GestureMapper">

    <select id="findNotUseMapping">
        SELECT g.*
        FROM gestures g
        WHERE NOT EXISTS (
            SELECT 1
            FROM mapping_items mi
            WHERE mi.gesture_id = g.id
              AND mi.mapping_id = #{mappingId}
        );
    </select>

</mapper>
