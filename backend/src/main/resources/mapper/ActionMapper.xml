<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.backend.mapper.ActionMapper">

    <select id="findNotUseMapping">
        SELECT a.*
        FROM actions a
        WHERE NOT EXISTS (
            SELECT 1
            FROM mapping_items mi
            WHERE mi.action_id = a.id
              AND mi.mapping_id = #{mappingId}
        );
    </select>

</mapper>
